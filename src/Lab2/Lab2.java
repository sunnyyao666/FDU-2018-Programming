package Lab2;

import javafx.application.Application;
import javafx.event.EventHandler;
import javafx.scene.Scene;
import javafx.scene.control.Label;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.scene.input.MouseEvent;
import javafx.scene.layout.HBox;
import javafx.scene.paint.Color;
import javafx.scene.text.Font;
import javafx.stage.Stage;

public class Lab2 {

    public static void main(String[] args) throws InterruptedException {
           /* Scanner input = new Scanner(System.in);
            System.out.println ("Enter the length of the cuboid");
            int length = input.nextInt();
            System.out.println("Enter the width of the cuboid");
            int width = input.nextInt();
            System.out.println("Enter the height of the cuboid");
            int height = input.nextInt();
            int area;
            area = 2 * (length * width + width * height + length * height) ;
            System.out.println("The surface area of the cuboid is " + area);
            System.out.println("Enter the radius of the sphere");
            float radius = input.nextFloat();
            double area2;
            area2 = 4 * Math.PI * radius * radius;
            System.out.println("The surface area of the sphere is " + area2);
            System.out.println("Enter the letter");
            char letter = input.next().charAt(0);
            if (letter <= 90) letter = (char) (letter+32);
            else letter = (char) (letter-32);
            System.out.println("The other form is " + letter);*/



            while (true ){
                System.out.println(1);
                Thread.sleep(2000);
            }




            }

        }
